# Приложение для управления принтерами (Print Manager Desktop)

Десктопное приложение для управления сетевыми принтерами и отправки документов на печать.

## Возможности

- Управление сетевыми принтерами
- Проверка доступности принтеров через TCP
- Отправка документов на печать через RAW-протокол (порт 9100)
- Кроссплатформенность (Windows, macOS, Linux)

## Технологии

- Electron - для создания кроссплатформенного десктопного приложения
- React - для создания пользовательского интерфейса
- TypeScript - для типизации и повышения надежности кода
- Material UI - для создания стильного и удобного интерфейса

## Системные требования

- Node.js v16 или новее
- npm v7 или новее

## Установка и запуск

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

### Сборка приложения

```bash
npm run build
```

### Создание установочного пакета

```bash
npm run dist
```

## Структура проекта

- `src/main` - Код основного процесса Electron
- `src/renderer` - Код рендерера (React-приложение)
  - `components` - Переиспользуемые компоненты
  - `pages` - Страницы приложения
  - `hooks` - Пользовательские хуки
  - `utils` - Вспомогательные функции
- `src/shared` - Код, общий для основного процесса и рендерера

## Работа с принтерами

Приложение использует RAW-протокол (порт 9100) для взаимодействия с сетевыми принтерами. Это позволяет отправлять данные напрямую на принтер без использования драйверов.

Поддерживаемые форматы файлов зависят от возможностей принтера, но обычно включают PDF, PostScript и некоторые другие форматы.

## Лицензия

MIT
